# 贡献砺境

感谢你参与砺境。当前公开仓库的主要用途是让队员查看、讨论和协作推进，不代表演示客户端已经具备生产能力。

## 开始之前

环境要求：Node.js `>=22.18.0`、pnpm `11.19.0`。

```powershell
pnpm install
pnpm client:serve
```

浏览器访问 `http://127.0.0.1:4187`。AI 私有试点默认关闭，不需要密钥也可以查看完整演示。

## 分支与提交

- `master` 只接受经过检查的合并提交。
- 功能使用 `feature/...`，修复使用 `fix/...`，文档使用 `docs/...`。
- 提交前运行 README 中列出的校验命令；涉及页面或动效时附截图和验证路径。
- 修改 API、事件、模块边界、AI 策略或数据分类时，同步更新对应文档和测试。

## Pull Request

PR 描述请写清楚：

- 改了什么，以及影响哪些页面或模块；
- 是演示能力、试点能力还是生产基线；
- 跑过哪些测试；
- 还剩哪些风险、依赖或人工审核项。

不要提交真实密钥、用户隐私、未经授权的素材、`node_modules`、本地工作树或 `.codegraph/`。

## 代码边界

客户端是演示和不可信边界，不能自行宣布掌握度、奖励或完成事实。正式业务状态必须由服务端领域模块通过版本化 `/api/v1` 契约结算。当前 `/api/v1/ai/assist` 和 `/api/v1/ai/review` 只是绑定本机的私有适配器，不应部署到公网。
